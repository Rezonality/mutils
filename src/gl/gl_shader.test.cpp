#include <catch2/catch.hpp>
#include "mutils/gl/gl_shader.h"

using namespace MUtils;

TEST_CASE("CompileShadersBadPath", "Compile")
{
    //auto pRet = gl_compile_shader(ShaderType::Vertex, "fobar");
    //REQUIRE(pRet->ok == false);
}
